language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install --upgrade pip
  - pip install poetry
  - poetry install

script:
  - nose2 --with-coverage --coverage-report xml

after_success:
  - coveralls
